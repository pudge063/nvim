# Основы vim/nvim

Это не про плагины и не про этот конфиг конкретно — это про сам vim:
базовые вещи, которые работают в любом vim/nvim где угодно. Хоткеи именно
этого конфига — в [KEYMAPS.md](KEYMAPS.md).

## Оглавление

- [Режимы](#режимы)
- [Движения (motions)](#движения-motions)
- [Операторы + текстовые объекты](#операторы--текстовые-объекты)
- [Копирование/вставка и регистры](#копированиевставка-и-регистры)
- [Visual mode и выделения](#visual-mode-и-выделения)
- [Перемещение и переупорядочивание строк](#перемещение-и-переупорядочивание-строк)
- [Undo / redo](#undo--redo)
- [Поиск и замена](#поиск-и-замена)
- [Повтор команды: точка](#повтор-команды-точка)
- [Метки (marks)](#метки-marks)
- [Макросы](#макросы)
- [Буферы, окна, вкладки — в чём разница](#буферы-окна-вкладки--в-чём-разница)
- [Командная строка](#командная-строка)

---

## Режимы

Главное отличие vim от привычных редакторов — режимы. Клавиши делают
разное в зависимости от того, в каком вы режиме. Внизу экрана обычно
видно текущий режим (или это делает lualine/statusline).

| Режим | Как войти | Что происходит | Как выйти |
|---|---|---|---|
| **Normal** | (режим по умолчанию) | Клавиши — это команды, не текст | — |
| **Insert** | `i`, `a`, `o`, `I`, `A`, `O` (см. ниже) | Печатаете текст как обычно | `Esc` (или `jk` в этом конфиге) |
| **Visual** | `v` | Выделяете текст посимвольно | `Esc` |
| **Visual Line** | `V` | Выделяете целыми строками | `Esc` |
| **Visual Block** | `Ctrl-v` | Выделяете прямоугольный блок (колонку) | `Esc` |
| **Command-line** | `:` | Вводите команду (`:w`, `:%s/.../...`) | `Enter` (выполнить) или `Esc` (отмена) |
| **Replace** | `R` | Печать заменяет символы под курсором, а не вставляет | `Esc` |
| **Terminal** | заход в окно терминала (`<C-\>` в этом конфиге) | Ввод идёт прямо в шелл | `Esc` — в normal (не закрывая процесс) |

Варианты входа в insert mode — они не взаимозаменяемы, у каждого своя роль:

| Клавиша | Куда встаёт курсор |
|---|---|
| `i` | Перед символом под курсором |
| `a` | После символа под курсором |
| `I` | В начало строки (первый непробельный символ) |
| `A` | В конец строки |
| `o` | Новая строка **под** текущей, курсор туда |
| `O` | Новая строка **над** текущей, курсор туда |

## Движения (motions)

Движения перемещают курсор. Сами по себе или в комбинации с операторами
(см. ниже) — тогда они определяют, **над чем** оператор сработает.

| Клавиши | Куда |
|---|---|
| `h` `j` `k` `l` | Влево / вниз / вверх / вправо |
| `w` | В начало следующего слова |
| `b` | В начало предыдущего слова |
| `e` | В конец текущего/следующего слова |
| `0` | В самое начало строки (колонка 0) |
| `^` | На первый непробельный символ строки |
| `$` | В конец строки |
| `gg` | В начало файла |
| `G` | В конец файла |
| `{число}G` или `:{число}` | На конкретную строку, например `42G` |
| `f{символ}` | К следующему вхождению символа в строке (например `fx` — к следующей `x`) |
| `t{символ}` | До следующего вхождения символа (встаёт перед ним) |
| `F{символ}` / `T{символ}` | То же самое, но назад |
| `;` / `,` | Повторить последний `f`/`t` вперёд / в обратную сторону |
| `%` | К парной скобке `(){}[]` |
| `{` / `}` | На абзац назад/вперёд (пустая строка = граница абзаца) |
| `Ctrl-d` / `Ctrl-u` | Прокрутка на полэкрана вниз/вверх |
| `Ctrl-f` / `Ctrl-b` | Прокрутка на весь экран вперёд/назад |
| `zz` | Прокрутить так, чтобы текущая строка была по центру экрана |

## Операторы + текстовые объекты

Вот это — суть vim. Оператор + движение/текстовый объект = команда.
Схема: `{оператор}{движение или текстовый объект}`.

Основные операторы:

| Оператор | Действие |
|---|---|
| `d` | Удалить (delete) |
| `c` | Изменить (change) — удаляет и сразу переводит в insert mode |
| `y` | Скопировать (yank) |
| `>` / `<` | Сдвинуть отступ вправо/влево |
| `=` | Автоотступ (через treesitter в этом конфиге) |
| `gu` / `gU` | В нижний / верхний регистр |

Примеры с движениями:

| Команда | Значит |
|---|---|
| `dw` | Удалить до конца слова |
| `d$` (или `D`) | Удалить до конца строки |
| `dd` | Удалить всю строку |
| `yy` | Скопировать всю строку |
| `cw` | Заменить слово (удалить + insert mode) |
| `>>` | Сдвинуть текущую строку отступом вправо |

**Текстовые объекты** — вместо движения указываете, ЧТО именно: слово,
скобки, кавычки, абзац. `i` = inner (внутри, без обрамления), `a` = around
(включая обрамление). Это то, ради чего стоит выучить vim по-настоящему —
особенно полезно в Python-коде:

| Команда | Значит |
|---|---|
| `ciw` | Заменить слово под курсором целиком |
| `di"` | Удалить содержимое строки **внутри** кавычек: `"курсор здесь"` → `""` |
| `da"` | Удалить строку **вместе** с кавычками |
| `ci(` или `cib` | Заменить содержимое скобок — идеально для аргументов функции: `foo(курсор)` → очищает всё между `(` и `)` |
| `di{` | То же для `{}` — например, содержимое dict-литерала |
| `dap` | Удалить абзац (параграф) целиком, с пустой строкой после |
| `dip` | Удалить абзац, но без пустой строки-разделителя |
| `cit` | Заменить содержимое между HTML/XML-тегами |

Реальный пример: курсор где угодно внутри `def foo(a, b, c):` между
скобками — `ci(` очищает `a, b, c`, оставляет `def foo():` с курсором
внутри скобок в insert mode, готовым печатать новые аргументы.

## Копирование/вставка и регистры

| Команда | Значит |
|---|---|
| `y` + движение/объект | Скопировать (yank) |
| `p` | Вставить **после** курсора/строки |
| `P` | Вставить **перед** курсором/строкой |
| `x` | Удалить символ под курсором (тоже попадает в регистр, как вырезание) |

Важный момент: `d`/`c`/`x` тоже кладут удалённое в регистр — как
«вырезать», а не просто стереть. Именно поэтому `dd` затем `p` двумя
командами переставляет строку местами с соседней.

**Регистры** — у vim их много, не один буфер обмена:

| Регистр | Что в нём |
|---|---|
| `"` (безымянный, по умолчанию) | Последнее удаление/копирование |
| `"0` | Последний именно **yank** (copy), не тронутый последующими `d`/`c` — удобно, если после copy сделали ещё delete, а вставить хотите скопированное |
| `"a` – `"z` | Именованные — сохраняются между операциями. `"ayy` — скопировать строку в регистр `a`, `"ap` — вставить из него |
| `"+` | **Системный буфер обмена** — то, чем можно поделиться с другими приложениями. `"+y` / `"+p` |
| `"_` | Чёрная дыра — удалить **не затирая** текущий регистр: `"_dd` удалит строку, но `p` после этого вставит то, что было скопировано раньше |

Пример: скопировали строку (`yy`), потом что-то удалили (`dd`) — обычный
`p` вставит уже удалённое, не скопированное. Чтобы вставить именно
скопированное — `"0p`.

## Visual mode и выделения

| Клавиши | Режим |
|---|---|
| `v` | Посимвольное выделение |
| `V` | Построчное выделение |
| `Ctrl-v` | Блочное (прямоугольное) выделение — колонка текста |
| `gv` | Восстановить последнее выделение |
| `o` (в visual mode) | Прыгнуть на другой конец выделения |

В visual mode операторы работают над выделенным: `d` удалит выделение,
`y` скопирует, `>`/`<` сдвинут отступ у всех выделенных строк сразу,
`gu`/`gU` — регистр, `J` — склеить выделенные строки в одну.

**Блочное выделение** (`Ctrl-v`) особенно полезно для правки колонки в
нескольких строках сразу: выделите блок, `I` — впечатать текст в начало
каждой строки блока, `A` — в конец, `Esc` — применится ко всем строкам
разом.

## Перемещение и переупорядочивание строк

| Команда | Значит |
|---|---|
| `dd` затем `p` | Переставить строку вниз на одну |
| `ddkP` | Переставить строку вверх на одну |
| `J` | Склеить следующую строку с текущей (через пробел) |
| `gJ` | То же самое, но без добавления пробела |
| `:m +1` | Переместить текущую строку на 1 вниз (`:move`) |
| `:m -2` | Переместить на 1 вверх |
| `:'<,'>m +3` | В visual mode: подвинуть выделенные строки на 3 вниз |

## Undo / redo

Подробнее уже разбирали, но для полноты:

| Команда | Значит |
|---|---|
| `u` | Отменить |
| `Ctrl-r` | Вернуть отменённое |
| `g-` / `g+` | По времени назад/вперёд (включая заброшенные ветки, не только текущую) |
| `U` | Отменить все правки на последней изменённой строке (легаси, редко нужно) |

`Ctrl-Z` в терминале — **не** отмена, а сворачивание процесса в фон
(job control шелла). См. [KEYMAPS.md](KEYMAPS.md#undo--история-изменений)
про визуальное дерево (`<leader>u`).

## Поиск и замена

| Команда | Значит |
|---|---|
| `/текст` затем `Enter` | Поиск вперёд по файлу |
| `?текст` | Поиск назад |
| `n` / `N` | Следующее / предыдущее совпадение (в направлении поиска) |
| `*` | Найти следующее вхождение слова под курсором |
| `#` | То же, но назад |
| `:s/old/new/` | Замена **первого** вхождения в текущей строке |
| `:s/old/new/g` | Замена **всех** вхождений в текущей строке |
| `:%s/old/new/g` | Замена по **всему файлу** |
| `:%s/old/new/gc` | То же, но с подтверждением каждой замены (`y`/`n`/`a`) |
| `:'<,'>s/old/new/g` | Замена только в выделении (visual mode → `:`, диапазон подставится сам) |

## Повтор команды: точка

`.` повторяет последнее изменение целиком. Это одна из самых недооценённых
команд в vim: сделали `ciw` + напечатали новое слово + `Esc` — дальше
просто `.` на другом слове повторит замену один в один. Комбинируется с
`f`/`;`: `f(ci(новый_текст<Esc>` затем `f(.` находит следующие скобки и
повторяет замену их содержимого.

## Метки (marks)

| Команда | Значит |
|---|---|
| `m{буква}` | Поставить метку, например `ma` |
| `` `{буква} `` | Прыгнуть точно на метку (позиция) |
| `'{буква}` | Прыгнуть на строку с меткой (начало строки) |
| `` `` `` | Вернуться туда, где были до последнего прыжка |
| `Ctrl-o` / `Ctrl-i` | Назад / вперёд по истории переходов (jumplist) — как «назад» в браузере |

## Макросы

Запись последовательности команд для повтора.

| Команда | Значит |
|---|---|
| `q{буква}` | Начать запись в регистр (например `qa`) |
| `q` | Остановить запись |
| `@{буква}` | Выполнить макрос из регистра, например `@a` |
| `@@` | Повторить последний выполненный макрос |
| `5@a` | Выполнить макрос `a` 5 раз подряд |

Пример: нужно обернуть 10 строк вида `name` в `"name",` — `qa` → `I"<Esc>A",<Esc>j` → `q`
записало макрос, дальше `9@a` применит к оставшимся девяти строкам.

## Буферы, окна, вкладки — в чём разница

Частая путаница у новичков:

- **Буфер** — открытый в памяти файл. Файлов может быть открыто сколько
  угодно, даже если видно только один. `:ls` / `<leader>fb` — список.
- **Окно** — видимая на экране область, показывающая буфer. Одно окно
  показывает один буфер. `Ctrl-w s` / `Ctrl-w v` — разбить окно
  горизонтально/вертикально (в этом конфиге переключение между окнами —
  `Ctrl-h/j/k/l`, см. [KEYMAPS.md](KEYMAPS.md#окна)).
- **Вкладка (tab)** — **не** то же самое, что вкладка в браузере/VSCode!
  Это отдельный **набор окон** (свой layout сплитов). `:tabnew` — новая
  вкладка, `gt`/`gT` — следующая/предыдущая.

То есть можно держать 20 файлов открытыми как буферы, при этом видеть
на экране 2 окна (сплит), и всё это на одной вкладке. Переключение между
буферами без сплита:

| Команда | Значит |
|---|---|
| `:bnext` / `:bprev` | Следующий / предыдущий буфер в том же окне |
| `:b {имя или часть имени}` | Переключиться на буфер по имени |
| `<leader>fb` | Список буферов через Telescope — обычно удобнее |

## Командная строка

Всё, что начинается с `:`. Самое частое:

| Команда | Значит |
|---|---|
| `:w` | Сохранить |
| `:q` | Закрыть окно (если последнее — выйти) |
| `:wq` или `:x` | Сохранить и закрыть |
| `:q!` | Закрыть без сохранения (отменить несохранённые правки) |
| `:e {путь}` | Открыть файл |
| `:e!` | Перечитать текущий файл с диска, отбросив несохранённые правки |
| `:%!{команда шелла}` | Прогнать весь буфер через внешнюю команду и заменить содержимым её вывода |
